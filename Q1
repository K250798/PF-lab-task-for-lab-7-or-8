#include <stdio.h>

int main() {
    int id[] = {1,1,2,3,4,4,4,5,5,6}, i;
    int n = sizeof(id) / sizeof(id[0]), j;
    int k = 0;

    
    for (i = 0; i < n; i++) {
        
        if (i == 0 || id[i] != id[i - 1]) {
            id[k] = id[i];
            k++;
        }
    }

    
    printf("Array after removing duplicates:\n");
    for (i = 0; i < k; i++) {
        printf("%d ", id[i]);
    }
    printf("\n");

    return 0;
}
